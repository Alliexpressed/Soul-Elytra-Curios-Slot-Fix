package com.example.soulelytraboostfix;

import net.neoforged.fml.common.Mod;

/**
 * No runtime logic needed here - the actual fix is the mixin registered via mods.toml. This
 * class only exists because NeoForge requires an @Mod entry point for every mod.
 */
@Mod(SoulElytraBoostFix.MOD_ID)
public class SoulElytraBoostFix {
    public static final String MOD_ID = "soulelytraboostfix";
}
